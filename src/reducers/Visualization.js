import { SET_PERSPECTIVE } from './../actions/ActionTypes'

const initialState = {

};

export default function Visualization(state = initialState, action) {
  switch(action.type){
    default:
      return state;
  }
}
